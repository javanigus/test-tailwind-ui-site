{
  "name": "test-tailwind-site",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel './src/**/*.html'",
    "build": "npx parcel './src/**/*.html --public-url ./'",
    "predeploy": "rm -rf dist .parcel-cache && npx parcel './src/**/*.html' --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "parcel": "^2.12.0",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4"
  }
}
